# R5 真实客户端入口观察记录 — 2026-05-22

## 背景

本记录用于补充 `design/28-R5工作通知入口排查清单-2026-05-22.md` 的真实客户端观察结果。

排查前已完成生产侧预检查与同步：

- `backend=active`
- `stream=active`
- `intake_worker=active`
- `pilot_metrics_timer=active`
- `health={"status":"ok","db":"connected"}`
- `template_set=True`
- `require_interactive=True`

生产侧记录见 `logs/2026-05-22-R5预检查与生产同步记录.md`。

## 观察时间

2026-05-22 19:54 CST

## 客户端观察

在 Frank 的钉钉 Mac 客户端中观察到两个独立入口：

1. `TDL管理助手`
2. `工作通知:保定市莲池区顾德教育培训学校有限公司 TDL`

打开 `TDL管理助手` 私聊后，可以看到最新交互卡片和按钮：

- `标记完成`
- `暂缓`
- `不是我的任务`

这说明机器人私聊入口和卡片渲染本身正常。

打开 `工作通知:保定市莲池区顾德教育培训学校有限公司 TDL` 后，落点是钉钉工作通知会话，不是 `TDL管理助手` 私聊。

其中最新 TDL 反馈消息为：

```text
已标记完成「组织斯坦教学员工集体磨课」已完成，不再提醒 继续处理请回到 TDL 助手私聊。
```

该消息确认了当前兜底文案已送达，但客户端中没有观察到可直接回跳到 `TDL管理助手` 私聊的按钮或链接。

## 当前结论

R5 当前仍不能判定为通过。

已确认：

- 工作通知反馈消息可以到达。
- 反馈消息中的兜底提示可见。
- `TDL管理助手` 私聊本身可打开，且卡片可见。

未通过点：

- 从工作通知会话不能直接回到 `TDL管理助手` 私聊。
- 工作通知内容没有提供可见的回跳按钮。

## 钉钉后台只读检查

2026-05-22 20:10 CST 继续进入钉钉开发者后台，只读检查 `TDL管理助手` 应用配置。

已确认：

- 当前后台应用名称为 `TDL管理助手`，状态为 `已上线`。
- 应用凭证页显示 `原企业内部应用AgentId` 为 `4584128030`，与生产 `.env` 中 `DINGTALK_AGENT_ID=4584128030` 一致。
- `Client ID` 与生产 `DINGTALK_APP_KEY` 指向同一个应用，`DINGTALK_OAUTH_CLIENT_ID` 在生产环境为空，未发现旧 OAuth 错配问题。
- 应用列表中 `TDL管理助手` 显示关联机器人：`TDL管理助手`。
- 机器人能力已启用，消息接收模式为 `Stream模式`。
- 最新上线版本为 `1.0.4`，上线时间为 `2026-05-17 02:45:17`。
- 版本详情中待发布能力只有 `应用信息` 和 `机器人`。
- 版本详情中的可见范围为部分员工，覆盖当前试点成员。
- `分享设置` 页只看到日历授权回调域名：`https://libu-h5.bdteach.cn/tdl/calendar/auth/callback`，没有观察到工作通知入口或应用首页跳转配置。

关键观察：

- `添加应用能力` 页中 `机器人` 是已配置状态。
- `网页应用`、`小程序`、`酷应用` 显示为可添加能力，不是当前上线能力。

因此，R5 目前更像是钉钉应用形态限制/入口能力缺失，而不是生产服务、`agent_id`、OAuth ClientID 或机器人 Stream 配置错误。

如果要让工作通知应用入口落到可控页面，下一步应评估是否给 `TDL管理助手` 增加 `网页应用` 能力，并把入口指向一个明确的 TDL 说明页或任务页。该动作会改变钉钉后台配置并需要发布新版本，执行前必须由 Frank 确认。

## 下一步

继续按 `design/28-R5工作通知入口排查清单-2026-05-22.md` 第五节排查钉钉后台应用配置。

重点确认：

- 钉钉是否支持工作通知点击后打开机器人私聊。
- 如果不支持私聊回跳，是否允许通过新增 `网页应用` 能力承接工作通知入口。
- 若采用网页入口，先设计最小 TDL 说明页或任务页，再由 Frank 确认后台发布。

在后台配置被确认前，不建议新增后端跳转逻辑。

## 入口页部署验证

2026-05-22 20:56 CST 已将 PR #117 的 TDL 网页入口部署到生产。

生产部署结果：

- `tdl-backend.service` active
- `tdl-stream-bot.service` active
- `tdl-intake-worker.service` active
- `tdl-pilot-metrics.timer` active
- `/health` 返回 `{"status":"ok","db":"connected"}`

公网路径验证：

- `https://libu-h5.bdteach.cn/tdl/` 返回 HTTP 200，页面包含 `TDL 管理助手`、`TDL管理助手` 和 `./health` 链接。
- `https://libu-h5.bdteach.cn/tdl/entry` 返回同一入口页。
- `https://libu-h5.bdteach.cn/tdl/health` 返回 `{"status":"ok","db":"connected"}`。

补充观察：

- `HEAD https://libu-h5.bdteach.cn/tdl/` 返回 405，因为后端只注册了 `GET /`，不影响浏览器访问和钉钉网页打开。
- 钉钉后台已进入 `添加应用能力` 页面，并确认 `网页应用` 仍处于可添加状态。
- 继续点击 `网页应用` 的 `添加` 会改变钉钉应用配置；该动作需要 Frank 明确确认后再执行。

当前可执行的下一步是：在钉钉后台给 `TDL管理助手` 添加 `网页应用` 能力，入口地址使用 `https://libu-h5.bdteach.cn/tdl/`，随后发布新版本并用真实客户端复测工作通知入口。

## 钉钉网页应用配置确认

2026-05-22 21:10 CST，Frank 已在钉钉后台保存并发布网页应用配置。

后台复核结果：

- 最新上线版本：`1.0.7`
- 版本状态：`已上线`
- 版本描述：`改地址`
- 待发布内容中包含：`工作台显示的应用能力` / `网页应用`
- 机器人能力仍显示为已启用
- 可见范围仍为原试点成员范围

网页应用配置复核结果：

- 企业内部应用 AgentId：`4584128030`
- 生效端：移动端、PC端
- 移动端首页地址：`https://libu-h5.bdteach.cn/tdl/`
- PC端首页地址：`https://libu-h5.bdteach.cn/tdl/`
- 管理后台地址：未设置

生产入口复核结果：

- `https://libu-h5.bdteach.cn/tdl/` 返回 HTTP 200。
- 页面包含 `TDL 管理助手`、`TDL管理助手` 和 `./health` 链接。
- 生产服务检查通过：backend、stream、intake worker、pilot metrics timer 均 active，`/health` 返回数据库 connected。

结论：钉钉后台网页应用入口配置已按预期完成并上线。后续仍需用真实客户端验证：工作台或工作通知入口是否实际打开该网页入口；历史工作通知消息不一定会 retroactively 改变落点，建议用新通知或工作台入口测试。

## 新工作通知入口测试

2026-05-22 21:12 CST，从生产环境向 Frank 发送了一条新的 `R5入口测试` 工作通知。

发送路径：

- 使用生产 `.env` 中的钉钉应用凭证。
- 通过 `DingTalkClient.send_work_markdown` 调用企业工作通知接口。
- 接收人：Frank 的钉钉 user_id。

真实客户端观察：

- 钉钉 Mac 客户端收到新会话条目：`工作通知:保定市莲池区顾德教育培训学校有限公司 R5入口测试`。
- 消息正文显示 `R5入口测试` 标题。
- 消息正文显示 `TDL 管理助手入口` 链接文本。
- 该消息来自 `TDL管理助手`。

未完成验证：

- 由于本机辅助点击权限限制，自动化工具无法稳定点击钉钉消息中的链接；`System Events` 被 macOS 拒绝辅助访问，Computer Use 对该窗口点击返回 `noWindowsAvailable`。
- 因此尚未确认点击该链接后是否打开 `https://libu-h5.bdteach.cn/tdl/`。

当前结论：

- `网页应用配置已上线`、`新工作通知已送达`、`通知中的网页入口链接已可见` 三项通过。
- `点击工作通知入口后的真实落点` 仍待 Frank 手动点击或授权辅助访问后复测，暂不把 R5 判定为完全通过。
