# PR-E 恢复开发入口与 R5 状态 — 2026-05-23

## 当前状态

截至本记录：

1. 日常 TDL 仍处于 `pilot`，Frank + 李珍可继续试用，不扩大推广。
2. 草稿低确认率/高忽略率已完成抽样诊断，主要受测试残留和一个已修复的 follow-up 误建错例污染。
3. 当前 open TDL 日历缺口为 0。
4. R5 已从广告页失败修正为受控网页入口，但仍不能一键回到 `TDL管理助手` 私聊。

## R5 当前判断

当前仓库可控能力：

- 工作通知反馈文案。
- `https://libu-h5.bdteach.cn/tdl/` 网页入口。
- 入口页说明用户回到 `TDL管理助手` 私聊继续处理。

当前仓库不可控能力：

- 钉钉工作通知应用图标点击后直接打开机器人私聊。
- 从 H5 页面构造稳定的机器人私聊 deeplink。

外部资料快速核对：

- 钉钉链接跳转规范显示普通 HTTP 链接默认打开为网页/H5，不天然等价于进入机器人私聊。
- 钉钉 JSAPI 存在打开会话类能力，但依赖会话 ID/JSAPI 环境，不等价于后端直接生成机器人私聊链接。

因此 R5 当前仍判为 `partial`：

- 作为 Frank + 李珍继续试用的阻塞项：否。
- 作为扩大推广前的阻塞项：是。

后续只有在钉钉后台或官方能力确认可稳定打开机器人私聊/任务上下文后，再继续实现；否则保留网页入口兜底。

## PR-E 入口建议

在 Frank 确认恢复开发优先级后，建议按以下顺序开小 PR：

1. **Pilot 新增样本质量读数**
   - 目标：新增真实草稿单独看，不再被 2026-05-19 到 2026-05-21 的测试残留污染。
   - 形式：扩展诊断脚本或 metrics 输出，标出 `pilot_*_cleanup` 取消原因。

2. **HITL intake diff 继续沉淀**
   - 目标：把 AI 初稿、用户 follow-up、最终字段差异变成稳定回归样本。
   - 边界：只记录，不自动生成 gold set。

3. **Postgres 单机 intake queue 生产观察**
   - 目标：确认当前 async intake 是否已实际产生 response time 数据。
   - 边界：不引入 Redis / Celery；先看 `intake_queue` 是否有真实流量和失败原因。

4. **按钮真实端验证包**
   - 目标：准备最小测试卡片，但真实点击仍需 Frank 在钉钉客户端完成。
   - 边界：避免一次性污染生产，必须用明确 `Codex验证` 标题和 allowlist 清理。

5. **会议分类 evaluator**
   - 目标：继续使用 5 月 gold set，跑严格挡板。
   - 边界：不把会议内容批量创建为正式 TDL。

## 当前不建议立即做

1. 不继续为 R5 猜测私聊 deeplink。
2. 不为了按钮真实验证批量创建大量测试 TDL。
3. 不因为历史草稿确认率低就重写 intake 主流程。
4. 不恢复知识层正式建表或上层插件开发。
