# 05 · 技术实现方案

> 与 06 的关系：06 定义 MVP 做什么，05 定义怎么做。技术选型服务于 MVP 范围。
> 与 07 的关系：07 开发计划里的文件路径和模块职责基于这里的技术骨架。

---

## 一、整体结构

系统跑在阿里云 ECS 上，通过飞书机器人与你交互。你的 Mac 不跑任何服务——合盖不影响。

```
飞书平台
  ├─ 机器人消息 ──→ 你的对话
  ├─ 日历 ────────→ TDL 自动写入日程
  └─ 消息推送 ←─── 提醒和卡片
        │
        ▼
阿里云 ECS (182.92.9.69)
  ├─ tdl-backend (FastAPI) ──→ Claude + ChatGPT
  ├─ PostgreSQL ──────────────→ TDL 数据
  ├─ APScheduler ─────────────→ 定时提醒
  └─ Nginx ──────────────────→ 反向代理
```

✅ 已确认：与励步钉钉 H5 项目部署在同一台 ECS，目录分离、服务分离、互不影响。

---

## 二、用什么技术，分别解决什么问题

### 必须有的

**① 云服务器（已有）**
- 阿里云 ECS 182.92.9.69，已在运行励步项目
- TDL 系统新增目录 `/opt/bots/tdl/backend`，新增 systemd 服务 `tdl-backend.service`
- 不需要额外买服务器

**② 后端框架：FastAPI**
- Python 异步框架，适合 AI API 调用密集的场景
- 自带数据校验和自动文档

**③ 数据库：PostgreSQL**
- 存 TDL 数据、管理层名单、审计日志、标签词典
- 开源免费，已有服务器上安装或复用

**④ 定时调度：APScheduler**
- 每日提醒、每周周报的定时任务
- 比 Celery 轻，MVP 不需要消息队列

**⑤ 飞书集成：官方 SDK**（生产）+ CLI（调试）
- 生产环境走飞书官方 Python SDK——稳定、错误处理好、支持高并发
- 调试运维时走飞书 CLI——快速验证、人类可读
- 直接复用 starplanet_ai 项目里的 `feishu_client.py`（250 行飞书 API 封装，与框架无关）

**⑥ AI 模型：多模型池 + 自动切换**

五模型组成池，按优先级自动切换：

| 优先级 | 模型 | 提供商 | 用途 | fallback 条件 |
|--------|------|--------|------|-------------|
| 1 | Claude（Opus/Sonnet） | Anthropic | 主分析引擎：TDL 分类、会议纪要拆解、周报生成 | 超时、限流、余额不足 |
| 2 | ChatGPT（GPT-5） | OpenAI | 流程自动化、Agent 编排 | 同上 |
| 3 | DeepSeek V3 Pro | DeepSeek | 中文场景补充、成本敏感型调用 | 同上 |
| 4 | 智谱 GLM | 智谱 AI | 国内网络兜底、低延迟 | 同上 |
| 5 | 通义千问 | 阿里云 | 同区域低延迟兜底（和 ECS 同机房） | 同上 |

**自动切换机制**：
```
调用模型 → 成功 → 返回结果
         → 失败（超时/限流/余额/网络）→ 记入故障列表
                                         → 自动尝试下一个优先级
                                         → 全部失败 → 返回"当前所有模型不可用，请稍后重试"
```

关键参数：
- 单模型超时：30 秒
- 故障冷却期：5 分钟（故障后 5 分钟内不再尝试该模型）
- 恢复探测：冷却期后下一次请求优先尝试，成功则移出故障列表
- 全模型故障通知：推送给少巍，标注 `[模型告警]`

💡 五模型 API Key 由少巍提供，存 `.env`。已确认：生产用 SDK，调试用 CLI。

### MVP 不需要的

| 东西 | 干什么的 | 什么时候加 |
|------|---------|-----------|
| Web 可视化仪表盘 | 网页看趋势图 | 第 2 阶段，你用了 2-4 周觉得需要时 |
| 备用模型（Hermes/OpenClaw） | 主模型挂了兜底 | 五模型池已覆盖，不需要额外备用 |
| 钉钉接入 | 钉钉用户也能用 | 推广期有钉钉用户需求时 |
| 语音交互 | 说话就能创建 TDL | 第 2 阶段，架构预留接口 |
| 向量数据库 | AI 长期记忆 | 第 2 阶段，数据量上来后再加 |
| OKR 对齐 | AI 关联战略目标 | 需要有真实维护的 OKR 数据源才行 |

---

## 三、从现有项目能复用哪些

| 资产 | 来源 | 复用方式 |
|------|------|---------|
| `feishu_client.py` | starplanet_ai 项目 | 直接复制——纯 HTTP 调用，跟框架无关 |
| AI 客户端架构 | starplanet_ai 的 `ai_client.py` | 模式参考——原版走 DeepSeek，新版换成 Claude+ChatGPT |
| 配置管理模式 | starplanet_ai 的 `config.py` | python-dotenv + 类继承模式 |
| API 蓝图组织方式 | starplanet_ai 的 `app.py` | 架构参考——FastAPI 用 Router 代替 Blueprint |
| ECS 部署模式 | 励步项目 | systemd + Nginx + git pull 部署，完全复用 |
| SSH Key | 已有 `id_ed25519` | 直接复用 |

---

## 四、密钥管理

⚠️ 真实密钥放 `.env` 文件（不提交 Git），业务参数放 `config/*.yaml`（可以提交 Git，不含敏感信息）。

```
项目根目录/
├── .env                    ← 不进 Git：API Key、数据库密码、飞书 App Secret
├── config/
│   ├── tdl_rules.yaml      ← 进 Git：置信度阈值、自动创建条件
│   ├── tag_dictionary.yaml ← 进 Git：标签词典
│   ├── escalation_policy.yaml ← 进 Git：升级策略参数
│   └── feishu_config.yaml  ← 进 Git：卡片样式、消息模板
```

---

## 五、AI 安全护栏

| 护栏 | 做法 |
|------|------|
| 不编造信息 | 标签词典做约束，AI 只能从已有的业务线/人员里选 |
| 高风险需确认 | 升级通知、批量提醒、修改他人任务 → 人工确认后才执行 |
| 成本控制 | 每人每日 token 上限，超了暂停 AI 调用 |
| 审计日志 | 每次创建、修改、催收、升级都留痕，可追溯 |
